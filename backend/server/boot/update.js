module.exports = function(app) {
  let Power = app.models.Power;

  /*Power.find({}, function(err, powers) {
    powers.forEach(function(power) {
      console.log(power.id);
      Power.replaceById(power.id, power, function(err, userreplace) {
      });
    });
  });*/
};
